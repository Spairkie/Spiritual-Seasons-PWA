[build]
  command = "npm run build"
  publish = "dist"

# The app uses a hash router (works unmodified on static hosts, no rewrite
# needed for in-app navigation) — this catch-all is just a safety net for
# anyone hitting a non-root path directly.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
